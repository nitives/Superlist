backend:
  name: Superlist
  repo: nitives/nitives.github.io   # Replace with your GitHub username and repository name

media_folder: "assets"
public_folder: "/media"

collections:
  - name: "posts"
    label: "Posts"
    folder: "_posts"
    create: true
    fields:
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Date", name: "date", widget: "datetime" }
      - { label: "Body", name: "body", widget: "markdown" }
